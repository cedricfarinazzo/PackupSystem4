#include <stdlib.h>
#include <stdio.h>
#include <err.h>
#include "bintree.h"


//Compression

void buildFrequenceList(char dataIN[])
{
    //char resultList
}

int main(int argc, char *argv[])
{
    if (argc != 2)
    {
        errx(4, "Argument number invalid !");
    }
    char *dataIN = argv[1];
    return 0;
}
